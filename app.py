"""Created on Mon May 18 22:19:58 2020@author: bensonsmathew"""from flask import Flask, render_template, request, sessionfrom flask_session.__init__ import Sessionfrom datetime import datetimeimport sqlite3import sockethostname = socket.gethostname()    IPAddr = socket.gethostbyname(hostname)print(f"Goto:\nhttp://{IPAddr}:5000\n")class employee_cl:    "Class to store employee details"    def __init__(self, e_num, first, last, title, shift, days_off, fte):        self.e_num = e_num        self.first = first        self.last = last        self.title = title        self.shift = shift        self.days_off = days_off        self.fte = fte        @property    def fullname(self)->str:        "Returns the full name of the emplyoee"        return f'{self.last.capitalize()}, {self.first.capitalize()}'        @property    def create_emp(self):        'Addes the emplyoee to the database'        conn = sqlite3.connect('employee.db')        c = conn.cursor()        c.execute(f"""                  insert into employee values (?,?,?,?,?,?,?,?,?,?,?)                  """,(self.e_num, self.first, self.last,self. title,                  self.shift, self.days_off, self.fte,                  datetime.now(), datetime.now(),                  'Admin', 'Admin'))        conn.commit()        conn.close()app = Flask(__name__)app.config['SESSION_PERMANENT'] = Falseapp.config['SESSION_TYPE'] = 'filesystem'Session(app)@app.route('/', methods=['POST','GET'])def index():    if session.get('notes') is None:        session['notes'] = []    if request.method == 'POST':        note = request.form.get('note')        print(note)        session['notes'].append(note)        print(session['notes'])    return render_template('hello.html', notes=session['notes'])@app.route('/employee')def employee():    names = ['Zen','Chopin', 'Beeth']    return render_template("EmployeePage.html", names=names)@app.route('/employeesubmit', methods=['GET','POST'])def emp_submit():    if request.method == 'GET':        return 'Please submit the form.'    e_num = request.form.get('e_num')    last = request.form.get('last')    first = request.form.get('first')    title = request.form.get('job_title')    shift = request.form.get('shift')    days_off = request.form.get('days_off')    fte = request.form.get('fte')    emp = employee_cl(e_num, last, first, title, shift, days_off, fte)    emp.create_emp    name = emp.fullname        return render_template('EmployFormSubmit.html', name=name)